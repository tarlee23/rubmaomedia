//JavaScript BOx JS Code
      let JSBox = 
      document.getElementById("JSBox");
      let JSButton = 
      document.getElementById("JSButton");

      JSButton.onclick = function() {
        JSBox.select();
        document.execCommand("copy");
        JSButton.innerText = "Codes Copied"
      }
      let JSSBox = 
      document.getElementById("JSSBox");
      let JSSButton = 
      document.getElementById("JSSButton");

      JSSButton.onclick = function() {
        JSSBox.select();
        document.execCommand("copy");
        JSSButton.innerText = "Codes Copied"
      }
      let JSSSBox = 
      document.getElementById("JSSSBox");
      let JSSSButton = 
      document.getElementById("JSSSButton");

      JSSSButton.onclick = function() {
        JSSSBox.select();
        document.execCommand("copy");
        JSSSButton.innerText = "Codes Copied"
      }
      let JSSSSBox = 
      document.getElementById("JSSSSBox");
      let JSSSSButton = 
      document.getElementById("JSSSSButton");

      JSSSSButton.onclick = function() {
        JSSSSBox.select();
        document.execCommand("copy");
        JSSSSButton.innerText = "Codes Copied"
      }
      let JSSSSSBox = 
      document.getElementById("JSSSSSBox");
      let JSSSSSButton = 
      document.getElementById("JSSSSSButton");

      JSSSSSButton.onclick = function() {
        JSSSSSBox.select();
        document.execCommand("copy");
        JSSSSSButton.innerText = "Codes Copied"
      }
      let JSSSSSSBox = 
      document.getElementById("JSSSSSSBox");
      let JSSSSSSButton = 
      document.getElementById("JSSSSSSButton");

      JSSSSSSButton.onclick = function() {
        JSSSSSSBox.select();
        document.execCommand("copy");
        JSSSSSSButton.innerText = "Codes Copied"
      }
      let JSSSSSSSBox = 
      document.getElementById("JSSSSSSSBox");
      let JSSSSSSSButton = 
      document.getElementById("JSSSSSSSButton");

      JSSSSSSSButton.onclick = function() {
        JSSSSSSSBox.select();
        document.execCommand("copy");
        JSSSSSSSButton.innerText = "Codes Copied"
      }
